{
  "name": "epsilon",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nx run-many -t build",
    "test": "nx run-many -t test",
    "lint": "nx run-many -t lint"
  },
  "devDependencies": {
    "nx": "^20.0.0"
  },
  "workspaces": [
    "modules/*"
  ]
}
