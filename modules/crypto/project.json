{
  "tags": [
    "scope:epsilon",
    "type:library"
  ],
  "targets": {
    "build": {
      "options": {
        "cwd": ".",
        "command": "./epsilon/epsilon --module epsilon.crypto --eval t"
      },
      "inputs": [
        "{projectRoot}/**/*.lisp",
        "{projectRoot}/module.lisp"
      ],
      "executor": "nx:run-commands",
      "cache": true
    },
    "test": {
      "options": {
        "cwd": ".",
        "command": "./epsilon/epsilon --test epsilon.crypto"
      },
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "{projectRoot}/**/*.lisp"
      ],
      "executor": "nx:run-commands",
      "cache": true
    }
  },
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "epsilon-crypto",
  "sourceRoot": "epsilon/modules/crypto",
  "implicitDependencies": [
    "epsilon-foreign",
    "epsilon-darwin",
    "epsilon-library"
  ],
  "projectType": "library"
}
