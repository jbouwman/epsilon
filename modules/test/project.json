{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "epsilon-test",
  "sourceRoot": ".",
  "projectType": "library",
  "tags": ["scope:epsilon", "type:library"],
  "implicitDependencies": ["epsilon-regex", "epsilon-digest"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "./epsilon --module epsilon.test --eval t"
      },
      "inputs": ["{projectRoot}/**/*.lisp", "{projectRoot}/module.lisp"]
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": ".",
        "command": "./epsilon --test epsilon.test"
      },
      "inputs": ["{projectRoot}/**/*.lisp"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "echo 'Lint: epsilon.test'"
      }
    }
  }
}
