{
  "tags": [
    "scope:epsilon",
    "type:library"
  ],
  "targets": {
    "build": {
      "options": {
        "cwd": ".",
        "command": "./epsilon/epsilon --module epsilon.http --eval t"
      },
      "inputs": [
        "{projectRoot}/**/*.lisp",
        "{projectRoot}/module.lisp"
      ],
      "executor": "nx:run-commands",
      "cache": true
    },
    "test": {
      "options": {
        "cwd": ".",
        "command": "./epsilon/epsilon --test epsilon.http"
      },
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "{projectRoot}/**/*.lisp"
      ],
      "executor": "nx:run-commands",
      "cache": true
    }
  },
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "epsilon-http",
  "sourceRoot": "epsilon/modules/http",
  "implicitDependencies": [
    "epsilon-core",
    "epsilon-json",
    "epsilon-crypto",
    "epsilon-darwin"
  ],
  "projectType": "library"
}
