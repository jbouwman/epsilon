{
  "tags": [
    "scope:epsilon",
    "type:library"
  ],
  "targets": {
    "build": {
      "options": {
        "cwd": ".",
        "command": "./epsilon/epsilon --module epsilon.digest --eval t"
      },
      "inputs": [
        "{projectRoot}/**/*.lisp",
        "{projectRoot}/module.lisp"
      ],
      "executor": "nx:run-commands",
      "cache": true
    },
    "test": {
      "options": {
        "cwd": ".",
        "command": "./epsilon/epsilon --test epsilon.digest"
      },
      "dependsOn": [
        "build"
      ],
      "inputs": [
        "{projectRoot}/**/*.lisp"
      ],
      "executor": "nx:run-commands",
      "cache": true
    }
  },
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "epsilon-digest",
  "sourceRoot": "epsilon/modules/digest",
  "implicitDependencies": null,
  "projectType": "library"
}
