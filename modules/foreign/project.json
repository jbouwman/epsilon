{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "epsilon-foreign",
  "sourceRoot": ".",
  "projectType": "library",
  "tags": ["scope:epsilon", "type:library"],
  "implicitDependencies": ["epsilon-parsing", "epsilon-library"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "./epsilon --module epsilon.foreign --eval t"
      },
      "inputs": ["{projectRoot}/**/*.lisp", "{projectRoot}/module.lisp"]
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "cwd": ".",
        "command": "./epsilon --test epsilon.foreign"
      },
      "inputs": ["{projectRoot}/**/*.lisp"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": ".",
        "command": "echo 'Lint: epsilon.foreign'"
      }
    }
  }
}
