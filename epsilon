#!/bin/sh
#
# Epsilon command-line interface
#
# Works from fresh checkout or system installation.
# Uses bundled SBCL if available, otherwise system SBCL.
#

set -eu

# Resolve symlinks to find the actual epsilon installation
SCRIPT="$0"
while [ -L "$SCRIPT" ]; do
    SCRIPT_DIR="$(cd "$(dirname "$SCRIPT")" && pwd)"
    SCRIPT="$(readlink "$SCRIPT")"
    # Handle relative symlinks
    case "$SCRIPT" in
        /*) ;;
        *) SCRIPT="$SCRIPT_DIR/$SCRIPT" ;;
    esac
done

# The directory where epsilon is located
# Handle both root-level (./epsilon) and bin-level (./bin/epsilon) layouts
SCRIPT_DIR="$(cd "$(dirname "$SCRIPT")" && pwd)"
if [ -f "$SCRIPT_DIR/module.sexp" ]; then
    # Script is at root level (development layout)
    export EPSILON_HOME="$SCRIPT_DIR"
elif [ -f "$SCRIPT_DIR/../module.sexp" ]; then
    # Script is in bin/ subdirectory (release layout)
    export EPSILON_HOME="$(cd "$SCRIPT_DIR/.." && pwd)"
else
    export EPSILON_HOME="$SCRIPT_DIR"
fi
EPSILON_BOOT="$EPSILON_HOME/src/epsilon.lisp"

# Detect boot script
if ! [ -f "$EPSILON_BOOT" ]; then
    echo "Error: epsilon boot script not found in $EPSILON_HOME" >&2
    exit 1
fi

# Detect platform for vendored SBCL
detect_platform() {
    OS="$(uname -s | tr '[:upper:]' '[:lower:]')"
    ARCH="$(uname -m)"
    case "$OS" in
        darwin) OS="darwin" ;;
        linux) OS="linux" ;;
        *) OS="unknown" ;;
    esac
    case "$ARCH" in
        x86_64|amd64) ARCH="x86_64" ;;
        arm64|aarch64) ARCH="arm64" ;;
        *) ARCH="unknown" ;;
    esac
    echo "${OS}-${ARCH}"
}

# Find SBCL: require vendored SBCL (custom fork with extended C-call conventions)
SBCL=""
SBCL_CORE_ARG=""
PLATFORM="$(detect_platform)"
VENDOR_SBCL="$EPSILON_HOME/vendor/sbcl/$PLATFORM/sbcl"
VENDOR_CORE="$EPSILON_HOME/vendor/sbcl/$PLATFORM/sbcl.core"

if [ -n "${EPSILON_SBCL:-}" ] && [ -x "$EPSILON_SBCL" ]; then
    # Nix-built SBCL: has SBCL_HOME compiled in, no core/home paths needed
    SBCL="$EPSILON_SBCL"
elif [ -x "$VENDOR_SBCL" ]; then
    SBCL="$VENDOR_SBCL"
    if [ -f "$VENDOR_CORE" ]; then
        SBCL_CORE_ARG="--core $VENDOR_CORE"
    fi
    # Set SBCL_HOME so SBCL can find vendored contribs (sb-posix)
    export SBCL_HOME="$EPSILON_HOME/vendor/sbcl/$PLATFORM"
else
    echo "Error: SBCL not found for platform: $PLATFORM" >&2
    echo "" >&2
    echo "Epsilon requires a custom SBCL fork (github:jbouwman/sbcl) with" >&2
    echo "extensions not present in upstream SBCL. The fork is not vendored;" >&2
    echo "build it locally with:" >&2
    echo "" >&2
    echo "    ./vendor/sbcl/build.sh" >&2
    echo "" >&2
    echo "Under Nix, 'nix develop' provides it automatically via sbcl-epsilon." >&2
    echo "Override with EPSILON_SBCL=/path/to/sbcl to point at another build." >&2
    exit 1
fi

# Parse debug, rebuild, and quiet flags before passing to SBCL
DEBUG_MODE="false"
REBUILD_BOOT="false"
QUIET_MODE="false"

# Check for our custom flags
while [ $# -gt 0 ]; do
    case "$1" in
        --debug)
            DEBUG_MODE="true"
            shift
            ;;
        --rebuild)
            REBUILD_BOOT="true"
            shift
            ;;
        --quiet)
            QUIET_MODE="true"
            shift
            ;;
        --heap)
            EPSILON_HEAP_MB="$2"
            shift 2
            ;;
        --heap=*)
            EPSILON_HEAP_MB="${1#--heap=}"
            shift
            ;;
        *)
            break  # Stop at first non-epsilon flag
            ;;
    esac
done
# Now $@ contains only the remaining arguments

# Force rebuild of bootstrap.fasl if requested
if [ "$REBUILD_BOOT" = "true" ]; then
    echo "Forcing rebuild of bootstrap cache..."
    rm -f "$EPSILON_HOME/_build/epsilon.core/bootstrap.fasl"
fi

# Note: SBCL version skew is handled in-image, not here.
# load-boot-fasl in src/boot.lisp catches SB-FASL::INVALID-FASL-VERSION
# (and other FASL-format failures), wipes the stale boot FASLs, and
# falls through to generate-boot-fasl to recompile from source.
# Per-module FASLs are recovered the same way by
# epsilon.loader::load-fasl-with-recovery.  No shell-side marker is
# needed.

SBCL_RUNTIME_ARGS="--noinform --dynamic-space-size ${EPSILON_HEAP_MB:-1024}"
if [ "$DEBUG_MODE" = "true" ]; then
    SBCL_TOPLEVEL_ARGS="--no-sysinit --no-userinit --quit"
else
    SBCL_TOPLEVEL_ARGS="--no-sysinit --no-userinit --disable-debugger --quit"
fi

# Capture user's working directory before changing to EPSILON_HOME
export EPSILON_CWD="$PWD"

cd "$EPSILON_HOME"

# Pass --quiet flag back to the Lisp side if quiet mode is enabled
if [ "$QUIET_MODE" = "true" ]; then
    if [ $# -eq 0 ]; then
        set -- "--quiet"
    else
        set -- "--quiet" "$@"
    fi
fi

# Note: SBCL args must be unquoted, user args must be quoted
if [ $# -eq 0 ]; then
    exec "$SBCL" $SBCL_CORE_ARG $SBCL_RUNTIME_ARGS \
         $SBCL_TOPLEVEL_ARGS \
         --load "$EPSILON_BOOT" \
         --eval "(epsilon.main:cli-run)" \
         --
else
    exec "$SBCL" $SBCL_CORE_ARG $SBCL_RUNTIME_ARGS \
         $SBCL_TOPLEVEL_ARGS \
         --load "$EPSILON_BOOT" \
         --eval "(epsilon.main:cli-run)" \
         -- "$@"
fi
